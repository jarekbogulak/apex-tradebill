# Expo Web dev: proxy via webpack to avoid CORS
# Leave unset to auto-use `/api` on web dev, or set explicitly:
# APEX_BASE_URL=/api

# Native or production web: use the real API (no trailing slash)
# APEX_BASE_URL=https://testnet.omni.apex.exchange/api

APEX_NETWORK=TESTNET

# API credentials (optional unless using private endpoints)
APEX_API_KEY=
APEX_API_SECRET=
APEX_API_PASSPHRASE=
APEX_L2_KEY=

# Defaults for symbol display
DEFAULT_SYMBOL=BTCUSDT
DEFAULT_SYMBOL_DASH=BTC-USDT
