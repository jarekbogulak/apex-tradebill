describe('ApeX symbol allowlist', () => {
  test('accepts only configured symbols from allowlist configuration', () => {
    throw new Error(
      'Pending implementation: enforce allowlist once configuration loader exists (Owner: Codex, target 2025-03-30).',
    );
  });

  test('rejects suspended symbols surfaced by exchange metadata', () => {
    throw new Error(
      'Pending implementation: integrate exchange metadata gating (Owner: Codex, target 2025-03-30).',
    );
  });
});
