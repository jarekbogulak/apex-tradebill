describe('Trade preview rounding rules', () => {
  test('rounds position cost to cents while preserving risk budgets', () => {
    throw new Error(
      'Pending implementation: wire rounding helpers into trade preview service (Owner: Codex, target 2025-04-12).',
    );
  });

  test('applies tick size floors to suggested stops', () => {
    throw new Error(
      'Pending implementation: map market metadata tick sizes into rounding logic (Owner: Codex, target 2025-04-12).',
    );
  });
});
