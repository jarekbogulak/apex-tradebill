/**
 * Contract placeholder: activate once /v1/trades/history is implemented.
 */

describe('GET /v1/trades/history', () => {
  it.todo('returns paginated history capped at 30 days');
  it.todo('exposes nextCursor for pagination when more data exists');
});
