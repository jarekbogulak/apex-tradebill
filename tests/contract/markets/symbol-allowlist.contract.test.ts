describe('GET /v1/markets/{symbol} allowlist enforcement', () => {
  test('rejects symbols not present in configured ApeX allowlist', () => {
    throw new Error(
      'Pending implementation: extend contract once allowlist error semantics are finalized (Owner: Codex, target 2025-03-30).',
    );
  });
});
