/**
 * Contract placeholder: convert todos once streaming transport is online.
 */

describe('GET /v1/stream/market-data (WebSocket)', () => {
  it.todo('emits market.snapshot messages matching the contract');
  it.todo('marks payloads as stale after the freshness window elapses');
});
