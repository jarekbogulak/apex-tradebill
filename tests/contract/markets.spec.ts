/**
 * Contract placeholder: activate once /v1/markets/{symbol} returns live data.
 */

describe('GET /v1/markets/{symbol}', () => {
  it.todo('returns symbol metadata in line with exchange precision rules');
  it.todo('yields a 404 error for suspended or unknown symbols');
});
