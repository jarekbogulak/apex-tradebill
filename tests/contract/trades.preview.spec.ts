/**
 * Contract placeholder: convert todos into executable tests once /v1/trades/preview exists.
 */

describe('POST /v1/trades/preview', () => {
  it.todo('returns trade outputs matching the documented schema');
  it.todo('rejects inconsistent stops with a validation error payload');
});
