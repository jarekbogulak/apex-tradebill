describe('Risk visualization', () => {
  test.skip('keeps entry, stop, and target markers in sync with calculator outputs', () => {
    // TODO bind visualization component to calculator store and market stream
    // (Owner: Codex, target 2025-03-22).
  });
});
