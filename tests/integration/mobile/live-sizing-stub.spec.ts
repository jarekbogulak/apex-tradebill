describe('Live sizing (stub)', () => {
  test('renders calculator with stubbed values from Fastify seed endpoints', () => {
    throw new Error(
      'Pending implementation: wire Expo calculator against stubbed /v1/trades/preview data (Owner: Codex, target 2025-03-01).',
    );
  });
});
