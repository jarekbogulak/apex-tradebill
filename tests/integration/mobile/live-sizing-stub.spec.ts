describe('Live sizing (stub)', () => {
  test.skip('renders calculator with stubbed values from Fastify seed endpoints', () => {
    // TODO wire Expo calculator against stubbed /v1/trades/preview data
    // (Owner: Codex, target 2025-03-01).
  });
});
