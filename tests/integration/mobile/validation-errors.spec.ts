describe('Validation error UX', () => {
  test('blocks calculation when stop/target inputs violate directional constraints', () => {
    throw new Error(
      'Pending implementation: ensure validation messages surface once calculator rules exist (Owner: Codex, target 2025-03-05).',
    );
  });
});
