describe('Validation error UX', () => {
  test.skip('blocks calculation when stop/target inputs violate directional constraints', () => {
    // TODO ensure validation messages surface once calculator rules exist
    // (Owner: Codex, target 2025-03-05).
  });
});
