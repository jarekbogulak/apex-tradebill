describe('Stale fallback and reconnect', () => {
  test.skip('surfaces stale indicator and schedules reconnect attempts after feed loss', () => {
    // TODO add stale detection heuristics and reconnect instrumentation
    // (Owner: Codex, target 2025-03-20).
  });
});
