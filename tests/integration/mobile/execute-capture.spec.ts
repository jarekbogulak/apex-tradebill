describe('Execute capture quickstart scenario', () => {
  test.skip('persists exactly one history entry per Execute press with optimistic UI states', () => {
    // TODO simulate valid calculator inputs, invoke Execute, and assert:
    // 1. Button enters saving/disabled state until API resolves.
    // 2. Exactly one trade history entry is appended per press even if the user taps rapidly.
    // 3. History reflects the latest calculator output payload and snapshot.
    // (Owner: Codex, target 2025-03-31 – see Quickstart acceptance step 4.)
  });
});
