describe('Live sizing (real feed)', () => {
  test('streams market snapshots and keeps calculator outputs synchronized', () => {
    throw new Error(
      'Pending implementation: connect Expo client to live market stream once WebSocket is available (Owner: Codex, target 2025-03-15).',
    );
  });
});
