describe('Live sizing (real feed)', () => {
  test.skip('streams market snapshots and keeps calculator outputs synchronized', () => {
    // TODO connect Expo client to live market stream once WebSocket is available
    // (Owner: Codex, target 2025-03-15).
  });
});
