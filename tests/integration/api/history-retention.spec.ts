describe('Trade history retention', () => {
  test('prunes calculations older than 30 days and exposes cursor pagination', () => {
    throw new Error(
      'Pending implementation: requires PostgreSQL retention job and history route wiring (Owner: Codex, target 2025-04-01).',
    );
  });
});
