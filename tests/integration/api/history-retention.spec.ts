describe('Trade history retention', () => {
  test.skip('prunes calculations older than 30 days and exposes cursor pagination', () => {
    // TODO requires PostgreSQL retention job and history route wiring
    // (Owner: Codex, target 2025-04-01).
  });
});
